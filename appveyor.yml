version: 1.0.{build}
image: Visual Studio 2017
configuration: Release
platform: Any CPU
cache: packages
before_build:
- nuget restore
build:
  project: NUnitToMSTest.sln
  verbosity: minimal
test:
  assemblies:
    only:
    - NUnitToMSTest.Tests*.dll